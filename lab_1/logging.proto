syntax = "proto3";

package logging;

service LoggingService {
    rpc LogMessage (LogRequest) returns (LogResponse);
}

message LogRequest {
    string id = 1;
    string msg = 2;
}

message LogResponse {
    string status = 1;
}
